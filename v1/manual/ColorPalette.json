{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://jamplus.com/schemas/ColorPalette.json",
  "title": "Color Palette Schema",
  "description": "Schema definition for a color palette",
  "$ref": "#/$defs/ColorPalette",
  "$defs": {
    "ColorPalette": {
      "title": "Color Palette",
      "description": "A defined set of colors with a given name.",
      "type": "object",
      "properties": {
        "uuid": {
          "type": "string",
          "format": "uuid",
          "description": "A unique UUID identifier for the palette."
        },
        "name": {
          "type": "string",
          "description": "The name for the color palette."
        },
        "colors": {
          "type": "array",
          "items": {
            "type": "string",
            "format": "uuid"
          },
          "description": "A list of color uuids found within the palette."
        }
      },
      "required": ["uuid", "name", "colors"],
      "additionalProperties": false
    }
  }
}
